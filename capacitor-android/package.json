{
  "name": "bfix-store-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "npx cap init 'B-Fix Software | AI Store' com.bfixsoftware.aistore --web-dir www",
    "add-android": "npx cap add android",
    "sync": "npx cap sync android",
    "build": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "release": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
